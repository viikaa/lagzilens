export const environment = {
  production: true,
  gasEndpoint: 'https://script.google.com/macros/s/YOUR_GAS_DEPLOYMENT_ID/exec',
  maxFileSizeBytes: 20 * 1024 * 1024,
  allowedMimeTypes: ['image/jpeg', 'image/png', 'image/heic', 'image/heif'],
};
